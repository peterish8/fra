export type DiscoverItem = { company_id: string; name: string; score?: number | null; explanation: string; state?: string };
export function DiscoverList({ items }: { items: DiscoverItem[] }) { return <main><h1>Discover</h1><p>Evidence-backed weekly research candidates. This is not investment advice.</p><p>Methodology: watchlist-v1 · candidates with insufficient coverage are excluded, not scored as zero.</p>{items.length ? <ul>{items.map((item) => <li key={item.company_id}><h2>{item.name}</h2><p>{item.score == null ? "Not enough data" : `${item.score}/100`}</p><p>{item.explanation}</p><span>{item.state ?? "ELIGIBLE"}</span></li>)}</ul> : <p role="status">No candidates qualify yet. Broaden filters or wait for fresh evidence.</p>}</main>; }
