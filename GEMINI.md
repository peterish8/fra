# Gemini Coding Instructions

Read and follow `AGENTS.md` first. It is the canonical repository instruction file.

Use the appropriate specifications under `docs/` as source of truth. If code and docs conflict, surface the discrepancy and resolve the specification before silently changing behavior.
