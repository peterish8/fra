---
phase: 01-foundation-quality
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [supabase/migrations/, backend/app/persistence/, .github/workflows/quality.yml, backend/app/observability/]
autonomous: true
requirements: [FND-03, DATA-01, DATA-02, DATA-03]
user_setup: []
must_haves:
  truths:
    - "The Truth Ledger baseline can be applied as a forward migration with constraints and indexes."
    - "RLS and backend ownership rules protect user-owned report data while service writes shared truth records."
    - "CI runs all required static and schema/API quality gates."
  artifacts:
    - path: "supabase/migrations/0001_truth_ledger.sql"
      provides: "Forward baseline schema derived from docs/03-data/schema.sql"
    - path: ".github/workflows/quality.yml"
      provides: "Required CI checks"
  key_links:
    - from: "report child access queries"
      to: "profiles/reports ownership"
      via: "RLS policy or secure backend owner join"
---

<objective>
Translate the schema blueprint into a validated migration baseline, establish RLS/immutability checks, and automate quality gates.

Purpose: Make durable truth and quality enforcement real before feature development depends on them.
Output: Forward migration, schema validation tests, ownership policies, and GitHub Actions workflow.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/03-data/04-DATABASE-SCHEMA.md
@docs/03-data/schema.sql
@docs/08-quality/12-TESTING-EVALUATION.md
@docs/08-quality/19-DEFINITION-OF-DONE.md
@docs/09-operations/14-DEPLOYMENT-CI-CD.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create the forward Truth Ledger migration</name>
  <files>supabase/migrations/0001_truth_ledger.sql, backend/app/persistence/</files>
  <read_first>docs/03-data/schema.sql, docs/03-data/04-DATABASE-SCHEMA.md</read_first>
  <action>Adapt the baseline SQL into the selected migration layout, preserving enums, UUID/timestamptz/decimal rules, foreign keys, checks, indexes, immutable/versioned tables, jobs, and audit events. Add only safe migration-tool metadata; do not silently remove schema objects.</action>
  <verify>Run SQL parse/migration validation against a disposable PostgreSQL/Supabase-compatible database where available, plus a schema contract test that checks all required tables/enums/index families.</verify>
  <acceptance_criteria>
    - All Truth Ledger objects named in the database spec exist in the migration.
    - Unknown financial values remain nullable and report versions/verification records are append-oriented.
    - Migration is forward-only and has no destructive reset step.
  </acceptance_criteria>
  <done>The database baseline is executable and contract-checked.</done>
</task>
<task type="auto">
  <name>Task 2: Complete ownership/RLS and immutable-record checks</name>
  <files>supabase/migrations/0001_truth_ledger.sql, backend/tests/security/, backend/tests/persistence/</files>
  <read_first>docs/03-data/04-DATABASE-SCHEMA.md, docs/07-security-governance/11-SECURITY-THREAT-MODEL.md</read_first>
  <action>Add authenticated policies for profiles/reports/report children through safe ownership joins or backend-controlled views, keep shared canonical writes service-only, and add tests for cross-user reads/writes plus historical record correction by superseding records.</action>
  <verify>Run RLS integration tests and migration/schema checks; if hosted Supabase is unavailable, report that live policy execution remains unverified.</verify>
  <acceptance_criteria>
    - A user cannot read/write another user's report or report child through a UUID.
    - Browser roles cannot write shared source/evidence truth records directly.
    - Immutable record tests reject in-place historical mutation paths.
  </acceptance_criteria>
  <done>Ownership and historical truth protections have executable tests.</done>
</task>
<task type="auto">
  <name>Task 3: Add CI quality workflow</name>
  <files>.github/workflows/quality.yml</files>
  <read_first>docs/08-quality/12-TESTING-EVALUATION.md, docs/09-operations/14-DEPLOYMENT-CI-CD.md</read_first>
  <action>Define CI jobs for pinned dependency install, frontend lint/typecheck/tests/build, backend format/lint/typecheck/tests, migration/schema validation, OpenAPI parsing, secret/dependency scanning, and targeted research-evaluation smoke tests.</action>
  <verify>Validate workflow syntax and run every available local job command.</verify>
  <acceptance_criteria>
    - Workflow contains every documented PR gate.
    - Live paid providers are not required for default CI.
  </acceptance_criteria>
  <done>Pull requests have a documented, runnable quality gate.</done>
</task>
</tasks>

<verification>
- [ ] Migration/schema validation passes.
- [ ] RLS/ownership tests pass or the unavailable hosted check is explicitly recorded.
- [ ] CI YAML and every referenced command validate.
</verification>

<success_criteria>
- Durable schema and ownership protections are in place.
- The project cannot claim CI readiness without the documented checks running.
</success_criteria>

<output>Create `.planning/phases/01-foundation-quality/01-02-SUMMARY.md` after execution.</output>
