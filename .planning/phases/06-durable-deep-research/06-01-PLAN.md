---
phase: 06-durable-deep-research
plan: 01
type: execute
wave: 1
depends_on: ["05-02"]
files_modified: [backend/app/jobs/, backend/app/domain/research/, backend/app/observability/, backend/tests/jobs/]
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03, RUN-04]
user_setup: []
must_haves:
  truths:
    - "Research jobs are durable, lease-based, retryable, and transaction-safe."
    - "Every pipeline stage checkpoints before transition and can resume after worker failure."
    - "Duplicate triggers and exhausted budgets produce explicit outcomes without duplicate versions."
  artifacts:
    - path: "backend/app/jobs/"
      provides: "Postgres-backed queue, worker lease, retry, and idempotency services"
    - path: "backend/app/domain/research/"
      provides: "Research run state machine and checkpoint orchestration"
  key_links:
    - from: "research run API"
      to: "jobs table"
      via: "idempotent enqueue and run identifier"
    - from: "worker stage"
      to: "research_run_stages"
      via: "transactional checkpoint before next state"
---

<objective>Make the research pipeline durable, resumable, idempotent, and cost-aware.</objective>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/01-technical/02-TRD.md
@docs/02-architecture/adr/ADR-005-durable-async-jobs.md
@docs/10-planning/18-TASK-BACKLOG.md
@docs/09-operations/13-OBSERVABILITY-OPERATIONS.md
</context>
<tasks>
<task type="auto"><name>Task 1: Build durable job queue and worker lease</name><files>backend/app/jobs/</files><read_first>docs/01-technical/02-TRD.md, docs/03-data/schema.sql</read_first><action>Implement enqueue/claim/heartbeat/complete/fail/cancel using transaction-safe locking, priority, available_at, lease_until, attempt_count, max_attempts, and exponential backoff+jitter. Record safe error summaries and provider/run IDs; never log secrets.</action><verify>Run queue tests for concurrent claim, lease expiry, retryable/permanent error, cancellation, priority, max attempts, and worker crash simulation.</verify><acceptance_criteria><list><li>Two workers cannot claim the same active job.</li><li>Expired lease becomes claimable without duplicate output records.</li></list></acceptance_criteria><done>Durable jobs can be safely operated across worker restarts.</done></task>
<task type="auto"><name>Task 2: Implement stage state machine and idempotent triggers</name><files>backend/app/domain/research/, backend/app/jobs/idempotency.py, backend/tests/jobs/</files><read_first>docs/01-technical/02-TRD.md, docs/04-api/05-API-SPEC.md</read_first><action>Implement QUEUED through SYNTHESIZING/COMPLETE stages with persisted checkpoints, partial/failure/cancelled terminal states, idempotency key scoping, and cost/page/search/deep-call/loop limits. Ensure repeated create/refresh cannot create duplicate report versions.</action><verify>Run integration tests that crash between stages, replay requests, exhaust budget, and restart from a checkpoint.</verify><acceptance_criteria><list><li>Stage output is persisted before advancing.</li><li>Repeated idempotency key returns the original run/resource.</li><li>Budget exhaustion records COST_BUDGET_EXCEEDED and useful partial state.</li></list></acceptance_criteria><done>Research execution has durable lifecycle semantics.</done></task>
</tasks>
<verification><list><li>Queue concurrency/lease tests pass.</li><li>Research state integration tests pass.</li><li>Run status endpoint exposes stage and degradation safely.</li></list></verification>
<success_criteria><list><li>Worker failure does not erase or duplicate truth.</li><li>Resource limits are enforced in orchestration.</li></list></success_criteria>
<output>Create `.planning/phases/06-durable-deep-research/06-01-SUMMARY.md` after execution.</output>
