---
phase: 01-foundation-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/, backend/, .env.example]
autonomous: true
requirements: [FND-01, FND-02, AUTH-01, AUTH-02]
user_setup: []
must_haves:
  truths:
    - "Frontend and FastAPI health endpoints run locally from validated configuration."
    - "An authenticated request can resolve its user identity and an unauthorized report access is rejected."
  artifacts:
    - path: "frontend/"
      provides: "Next.js TypeScript application shell with strict checks"
    - path: "backend/app/config/"
      provides: "Typed server configuration and secret-safe validation"
    - path: "backend/app/api/"
      provides: "Health, identity, and authorization boundaries"
  key_links:
    - from: "frontend"
      to: "backend /v1"
      via: "server-side API client using configured base URL"
    - from: "backend auth dependency"
      to: "report owner checks"
      via: "validated Supabase identity context"
---

<objective>
Bootstrap the frontend/backend application boundary, typed configuration, health surfaces, and authenticated request context.

Purpose: Establish the smallest runnable foundation before any provider or research behavior is added.
Output: A strict Next.js app, typed FastAPI app, safe `.env.example`, `/health`, `/v1/me`, and reusable authorization dependency.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@README.md
@docs/01-technical/02-TRD.md
@docs/02-architecture/03-ARCHITECTURE-HLD.md
@docs/04-api/05-API-SPEC.md
@docs/07-security-governance/11-SECURITY-THREAT-MODEL.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Bootstrap strict frontend and FastAPI shells</name>
  <files>frontend/, backend/, .env.example</files>
  <read_first>MASTER-BUILD-PROMPT.md, docs/01-technical/02-TRD.md</read_first>
  <action>Create the Next.js App Router TypeScript shell with Tailwind/shadcn-ready configuration and strict type/lint scripts. Create the typed FastAPI package with `/health` and `/v1/health`, Python typing, Pydantic settings, and a testable app factory. Keep frontend provider-free and backend provider-neutral.</action>
  <verify>Run the frontend lint/typecheck/build and backend format/lint/typecheck/unit commands defined by the new package scripts.</verify>
  <acceptance_criteria>
    - `frontend/package.json` exposes lint, typecheck, test, and build scripts.
    - `backend` exposes a FastAPI app factory and health route returning a stable JSON status.
    - No provider secret is referenced by frontend code.
  </acceptance_criteria>
  <done>Both application shells start locally and their health checks pass.</done>
</task>
<task type="auto">
  <name>Task 2: Add typed configuration and secret-safe request logging</name>
  <files>backend/app/config/, backend/app/observability/, .env.example</files>
  <read_first>docs/01-technical/02-TRD.md, docs/09-operations/13-OBSERVABILITY-OPERATIONS.md, docs/07-security-governance/11-SECURITY-THREAT-MODEL.md</read_first>
  <action>Define environment names for Supabase, API URLs, provider placeholders, budgets, and logging without values. Validate required settings at startup, add request IDs, JSON logs, duration/status fields, and redaction for bearer tokens, cookies, API keys, and sensitive headers.</action>
  <verify>Run configuration unit tests for missing/valid settings and an API test that confirms a request ID is returned without secrets in captured logs.</verify>
  <acceptance_criteria>
    - `.env.example` contains names only and no real credential-looking values.
    - Logs contain request ID/status/duration fields and redact secret headers.
    - Invalid configuration fails with a human-safe error.
  </acceptance_criteria>
  <done>Configuration and observability behavior is typed, tested, and safe by default.</done>
</task>
<task type="auto">
  <name>Task 3: Implement Supabase identity and reusable authorization boundary</name>
  <files>frontend/lib/auth/, backend/app/security/auth.py, backend/app/api/dependencies.py, backend/tests/</files>
  <read_first>docs/04-api/05-API-SPEC.md, docs/03-data/04-DATABASE-SCHEMA.md, docs/07-security-governance/11-SECURITY-THREAT-MODEL.md</read_first>
  <action>Wire the official Supabase session/JWT verification boundary server-side and expose `/v1/me`. Add a reusable current-user dependency and owner-check helper that returns stable `UNAUTHENTICATED`/`FORBIDDEN` errors. Do not expose the service-role key to the browser.</action>
  <verify>Run auth unit/integration tests covering missing, malformed, valid, and wrong-owner identities.</verify>
  <acceptance_criteria>
    - `/v1/me` requires authentication and returns the authenticated profile identity.
    - Owner checks are reusable by every user-owned endpoint.
    - Tests prove another user cannot pass a report UUID to access private data.
  </acceptance_criteria>
  <done>Authenticated identity and object authorization are available as shared backend boundaries.</done>
</task>
</tasks>

<verification>
- [ ] Frontend lint, typecheck, tests, and build pass.
- [ ] Backend format, lint, typecheck, and tests pass.
- [ ] Health and `/v1/me` contract tests pass.
</verification>

<success_criteria>
- The local stack is runnable and secrets remain server-side.
- Authenticated identity and owner authorization are testable primitives.
</success_criteria>

<output>Create `.planning/phases/01-foundation-quality/01-01-SUMMARY.md` after execution.</output>
