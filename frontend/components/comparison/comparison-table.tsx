"use client";
export type ComparisonMetric = { metric: string; values: { company_id: string; value: number | string | null; claim_version_id?: string | null }[]; warning?: string | null; compatible: boolean };
export function ComparisonTable({ companies, metrics }: { companies: string[]; metrics: ComparisonMetric[] }) { return <section className="comparison-panel" aria-labelledby="comparison-title"><header><h1 id="comparison-title">Compare companies</h1><p>Only compatible normalized metrics are ranked. Cohort and entity warnings remain visible.</p></header><div className="comparison-table-wrap"><table><thead><tr><th>Metric</th>{companies.map((company) => <th key={company}>{company}</th>)}</tr></thead><tbody>{metrics.map((row) => <tr key={row.metric}><th scope="row">{row.metric}</th>{row.values.map((item, index) => <td data-company={companies[index]} key={item.company_id}><span className="comparison-value">{item.value == null ? "Unknown" : item.value}</span>{row.warning ? <small> · {row.warning}</small> : null}{item.claim_version_id ? <button type="button">Inspect evidence</button> : null}</td>)}</tr>)}</tbody></table></div></section>; }
