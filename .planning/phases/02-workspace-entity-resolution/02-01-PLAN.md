---
phase: 02-workspace-entity-resolution
plan: 01
type: execute
wave: 1
depends_on: ["01-02"]
files_modified: [backend/app/domain/reports/, backend/app/api/reports.py, frontend/app/research/, frontend/components/research/]
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03]
user_setup: []
must_haves:
  truths:
    - "A user can create a report workspace with subject, focus, and depth."
    - "A user can list/search/open/soft-delete only their own workspaces."
  artifacts:
    - path: "backend/app/domain/reports/"
      provides: "Report service and persistence contracts"
    - path: "frontend/components/research/"
      provides: "Research creation and library/sidebar UI"
  key_links:
    - from: "research creation form"
      to: "POST /v1/reports"
      via: "validated request and returned report_id"
    - from: "report list"
      to: "GET /v1/reports"
      via: "owner-scoped cursor/filter query"
---

<objective>
Implement the persistent report workspace vertical slice: create, list/search, open, and soft-delete.

Purpose: Give the product a durable user-facing container before research runs are attached.
Output: Report domain service, protected REST endpoints, library/sidebar, and creation flow.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/00-project/01-PRD.md
@docs/03-data/04-DATABASE-SCHEMA.md
@docs/04-api/05-API-SPEC.md
@docs/04-api/openapi.yaml
@docs/06-product-design/09-UI-UX-SPEC.md
</context>

<tasks>
<task type="auto"><name>Task 1: Build report domain/API</name><files>backend/app/domain/reports/, backend/app/api/reports.py, backend/tests/reports/</files><read_first>docs/04-api/05-API-SPEC.md, docs/03-data/schema.sql</read_first><action>Define typed create/list/detail/delete DTOs, owner-scoped service methods, cursor pagination, q/status/company filters, soft deletion, stable errors, and Idempotency-Key behavior for create. Keep route handlers thin.</action><verify>Run report API tests for create/list/detail/filter/soft-delete/idempotent repeat/wrong owner.</verify><acceptance_criteria><list><li>`POST /v1/reports` returns 201 with stable report_id and DRAFT.</li><li>Deleted workspaces disappear from normal lists without deleting shared evidence.</li><li>Every endpoint uses the shared authorization boundary.</li></list></acceptance_criteria><done>Protected report CRUD is backed by PostgreSQL contracts and tests.</done></task>
<task type="auto"><name>Task 2: Build library and creation UI</name><files>frontend/app/research/, frontend/components/research/</files><read_first>docs/06-product-design/09-UI-UX-SPEC.md, docs/06-product-design/10-DESIGN-SYSTEM.md</read_first><action>Create the research home/library, report sidebar, search/filter controls, and creation form for company query, optional country/ticker/domain, focus chips, comparison placeholder, and Fast/Standard/Deep depth. Implement loading, validation, empty, error, and delete feedback.</action><verify>Run component tests and browser tests for create, list, open, filter, and soft-delete states.</verify><acceptance_criteria><list><li>Submitted form renders the created workspace and status DRAFT.</li><li>Sidebar never shows another user's report.</li><li>Status and errors use text/icons, not color alone.</li></list></acceptance_criteria><done>A user can manage report workspaces from the UI.</done></task>
</tasks>

<verification>
- [x] Backend report tests pass.
- [x] Frontend lint/typecheck/component/E2E checks pass.
- [x] OpenAPI and API examples remain synchronized.
</verification>
<success_criteria><list><li>Report workspaces persist and are owner-scoped.</li><li>Library and creation UI handle empty/loading/error states.</li></list></success_criteria>
<output>Create `.planning/phases/02-workspace-entity-resolution/02-01-SUMMARY.md` after execution.</output>
